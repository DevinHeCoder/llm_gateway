# Web 框架
fastapi==0.112.2
uvicorn==0.30.6
pydantic==2.8.2

# HTTP 客户端（调用 vLLM）
httpx==0.27.0

# 配置
pyyaml==6.0.2
python-dotenv==1.0.1

# 缓存 & 分布式限流
redis==5.0.8

# 监控（可选，Prometheus 导出）
prometheus-client==0.20.0

# 测试
pytest==8.3.2
pytest-asyncio==0.24.0
httpx==0.27.0
